## Q3.3

See [q3_3.cpp](./ch3/q3_3.cpp). A couple of notes:

1. I forgot the syntax for inheritance in C++. That was embarrassing.
2. I forgot how to call the super class. That was also embarrassing.
3. Using `stacks.erase(stacks.begin() + stackNum)` made `popAt()` very easy.

`push()`, `pop()`, and `popAt()` are all O(1) time. `toString()` is O(n) time.

## Q3.4

See [q3_4.cpp](./ch3/q3_4.cpp). `push()` is O(1), and `pop()` is O(1) amortized. `toString()` is O(n). I don't think we can do better than that.